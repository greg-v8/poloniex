#2016:8:22:23:7:58
sudo: required

language: nodejs

node:
 - '6'

services:
 - docker

branches:
 except:
 - develop
 - master

before_script:
 - cd ./trollbox/
 - npm install
script:
 - node validate.js

after_success:
 - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
 - docker build -f Dockerfile -t gregv8/trollbox:0.1.0 .
 - docker push gregv8/trollbox
# - git request-pull 0.1.0 $TRAVIS_REPO_SLUG version:0.1.0